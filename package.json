{
  "name": "nextjs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build && next export && next-sitemap --config next-sitemap.js",
    "start": "next start",
    "lint": "next lint",
    "export": "next export",
    "deploy": "next build && next export && next-sitemap --config next-sitemap.js && git add . && git commit -m 'release' && git push origin main"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.3",
    "next": "13.1.6",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
    "next-sitemap": "^4.0.9",
    "serve": "^14.2.0"
  }
}
